class treeclass{
    constructor (x,y,){
    var options= {
    'restitution': 0.1,
    'friction' : 0.8,
    'density' :0.8
    }
}
}
this.image=loadImage('IMAGES/Plucking mangoes/tree.js');
this.body=Bodies.rectangele(x,y,width,height,options);
this.width=width;
this.height=height;
this.image=loadImage('IMAGES/Plucking mangoes/mango.png');
World.add (world, thisbody);{
}
display() 
 var angle= this.body.angle;
 Push();
 translate (This.body.position.x,this.body.position.y);
 rotate(angle);
 imageMode('CENTER');
 image(this.image,0,0,this.width,this.height);
 Pop() ;






